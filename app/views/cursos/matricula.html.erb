<% form_for(@cursos) do |f| %>
  <%= f.error_messages %>
  <%= add_estudiante_link "Estudiante nuevo" %>
  <%= f.label :materia_id %><br />
  <%= f.text_field :area_tot %>
<%= f.submit 'Enviar' %>
<% end %>